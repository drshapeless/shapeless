// The Metal module is not a binding to the Metal API, instead a
// hand-written layer between objc and Jai. The aim is to not write
// any logical code in objc, but to make all the API call in objc and
// expose a function to Jai, like a binding, but not in a one-to-one
// way.
